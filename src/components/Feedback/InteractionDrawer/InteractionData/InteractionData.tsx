import { PatientId, ServiceId } from '../../../../api/types/types'
import './InteractionData.css'

const InteractionData = ({
  serviceId,
  patientId,
  interactionStart,
}: {
  serviceId: ServiceId
  patientId: PatientId
  interactionStart: Date
}) => {
  // return <div className="InteractionData">Datos</div>
  return null
}

export default InteractionData
