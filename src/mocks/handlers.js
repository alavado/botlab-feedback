import { rest } from 'msw'

export const handlers = [
  // rest.get('https://api.botlab.cl/answers_es', (req, res, ctx) => {
  //   return res(
  //     ctx.status(200),
  //     ctx.json({
  //       x: 'y',
  //     }),
  //   )
  // })
]